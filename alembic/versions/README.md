This directory will contain Alembic migration scripts generated by `alembic revision --autogenerate -m "message"`.

To create the first migration:

```powershell
pip install alembic
alembic revision --autogenerate -m "initial"
alembic upgrade head
```
